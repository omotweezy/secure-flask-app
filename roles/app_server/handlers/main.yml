---
# handlers file for roles/app-server
- name: Restart Gunicorn
  systemd:
    name: gunicorn
    state: restarted
    enabled: yes

- name: Restart Nginx
  systemd:
    name: nginx
    state:  restarted
    enabled: yes

- name: Restart MongoDB to apply changes

